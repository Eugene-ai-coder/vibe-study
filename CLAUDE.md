# 🏛️ 프로젝트 표준 (Project Standards)

## 1. 👤 사용자 프로필 및 협업 방식
- **사용자:** 27년 경력의 설계 전문가. 아키텍처와 구조적 의사결정의 주도권을 가짐.
- **Claude의 역할:** 설계자의 의도를 실체화하는 능동적 구현자.
- **소통 원칙:** 설명은 **구조 중심**으로 요약(계층·책임·흐름 위주). 라인 단위 설명 지양.
- **의사결정:** 구조적 선택이 필요할 때는 대안을 먼저 제안하고 설계자의 승인 후 진행.
- **수정 방식:** 주석 처리나 백업 파일 생성 금지. 항상 가장 깔끔한 최신 코드로 덮어쓰며, 이력은 Git으로 관리.

## 2. 🛠 코딩 원칙
- **아키텍처:** Controller → Service → Repository 표준 레이어 준수.
- **분리 원칙:** REST API(@RestController)와 웹 화면(@Controller)은 엄격히 분리.
- **미니멀리즘:** 불필요한 추상화 금지. 현재 요구사항에 맞는 최소 구조 유지.
- **에러 응답:** `GlobalExceptionHandler`를 통한 표준 JSON 응답 형식 준수.

## 🎨 3. UI/UX 가이드라인
- **Theme:** 배경색 `#F8FAFC`, 강조색 `#2563EB`
- **Style:** 카드(흰색, `rounded-lg`, 연한 그림자), 입력/버튼(높이 `h-8`, `border-gray-300`)
- **Layout:** 상단(조회조건) → 중간(입력폼/그리드) → 하단(플로팅 액션바)

## 📐 4. 규칙 파일 참조
- **Frontend 작업 시:** `docs/frontend-rules.md` 읽기
- **Backend 작업 시:** `docs/backend-rules.md` 읽기
- **용어 불명확 시:** `docs/glossary.md` 확인
